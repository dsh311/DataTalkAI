<Window x:Class="DataTalkAI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataTalkAI"
        mc:Ignorable="d"
        Title="DataTalkAI" Height="542" Width="800">

    <Grid Background="#050F1F">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1" Margin="0,0,0,40">

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Completion -->
            <TextBox x:Name="CompletionTextBox"
                 Grid.Row="0"
                 TextWrapping="Wrap"
                 Text="TextBox"
                 Margin="0,40,0,10"/>

            <!-- Input row container -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Prompt -->
                <TextBox x:Name="PromptTextBox"
                     Grid.Column="0"
                     TextWrapping="Wrap"
                     Height="57"
                     KeyDown="PromptTextBox_KeyDown"/>

                <!-- Send button -->
                <Button Grid.Column="1"
                    Click="ChatButton_Click"
                    Margin="10,0,0,0">
                    <Image Source="pack://application:,,,/Assets/SendBtn.png"
                       Width="92"
                       Height="56"/>
                </Button>

            </Grid>

        </Grid>
    </Grid>

</Window>
